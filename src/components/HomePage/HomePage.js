import { HomePageTitle } from "./HomePage.styled"

export const HomePage = () => {
  return (
    <HomePageTitle>
      The phone book manager welcomes you. If you are registered, login, if not, register.
    </HomePageTitle>
  );
}