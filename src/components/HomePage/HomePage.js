import { HomePageTitle } from "./HomePage.styled"

export const HomePage = () => {
  return (
    <HomePageTitle>
      The phone book manager welcomes you.<br/>If you are registered, login,<br/> if not, register.
    </HomePageTitle>
  );
}